const menu=document.getElementById('menu');const nav=document.getElementById('nav');menu.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('show');revealObserver.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -40px'});document.querySelectorAll('.reveal').forEach(el=>revealObserver.observe(el));
const counters=document.querySelectorAll('[data-count]');const counterObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;const el=entry.target,target=Number(el.dataset.count),duration=1300,start=performance.now();function tick(now){const p=Math.min((now-start)/duration,1),ease=1-Math.pow(1-p,3);el.textContent=Math.floor(target*ease);if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick);counterObserver.unobserve(el)})},{threshold:.6});counters.forEach(el=>counterObserver.observe(el));
function updateClock(){const now=new Date();const time=new Intl.DateTimeFormat('en-PK',{timeZone:'Asia/Karachi',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:true}).format(now);const date=new Intl.DateTimeFormat('en-PK',{timeZone:'Asia/Karachi',weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(now);document.getElementById('time').textContent=time;document.getElementById('date').textContent=date}updateClock();setInterval(updateClock,1000);document.getElementById('year').textContent=new Date().getFullYear();
window.addEventListener('scroll',()=>{const h=document.documentElement.scrollHeight-window.innerHeight;document.getElementById('progress').style.width=`${h>0?(window.scrollY/h)*100:0}%`;const sections=[...document.querySelectorAll('main section[id]')];const y=window.scrollY+140;sections.forEach(s=>{const link=nav.querySelector(`a[href="#${s.id}"]`);if(link)link.classList.toggle('active',y>=s.offsetTop&&y<s.offsetTop+s.offsetHeight)})},{passive:true});
const glow=document.querySelector('.cursor-glow');window.addEventListener('pointermove',e=>{if(innerWidth>900){glow.style.transform=`translate3d(${e.clientX-150}px,${e.clientY-150}px,0)`}});const stage=document.getElementById('stage');window.addEventListener('pointermove',e=>{if(innerWidth>900){const x=(e.clientX/innerWidth-.5)*10,y=(e.clientY/innerHeight-.5)*7;stage.style.transform=`rotateY(${x*.35}deg) rotateX(${-y*.25}deg)`}});window.addEventListener('pointerleave',()=>stage.style.transform='');
document.getElementById('quoteForm').addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget);const message=`Hello Kashif Software Solutions & Consultancy. Name: ${f.get('name')}. Contact: ${f.get('contact')}. Service: ${f.get('service')}. Project: ${f.get('message')}`;document.getElementById('formNote').textContent='Opening WhatsApp with your enquiry...';window.open(`https://wa.me/923219224285?text=${encodeURIComponent(message)}`,'_blank')});
